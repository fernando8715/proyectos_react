import React from 'react'
import ReactDOM from 'react-dom/client'
import {AppGastoSemanal} from './AppGastoSemanal.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppGastoSemanal />
  </React.StrictMode>,
)
